
#include <iostream>
#include <deque>
#include <vector>
using namespace std;

int main()
{
    int n, k;
    cin >> n >> k;

    vector<int> a(n);
    for (auto &ele : a)
        cin >> ele;

    deque<int> q;
    vector<int> ans;

    for (int i = 0; i < k; i++)
    {
        while (!q.empty())
        {
            /* code */
        }
    }
}